# Shared Interfaces

Define the contract between the generation side (Person A) and the
execution/evaluation side (Person B) here BEFORE building against each
other's stages.

## Spec Draft (output of spec_generator, input to repair_loop)
TODO: define fields (e.g. pluscal_text, source_refs, invariants, metadata)

## Lint Result (output of repair_loop)
TODO: define fields (e.g. success, errors, iterations_used)

## TLC Result (output of model_checker)
TODO: define fields (e.g. violated, counterexample_trace, stats)
